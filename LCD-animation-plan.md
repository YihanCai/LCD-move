# ESP32-C6 + 1.3" ST7789 LCD 动画方案

> 目标：在 240x240 的 ST7789 LCD 上实现"一个小人从左下角爬上山坡、跑到右上角"的动态特效。
> 开发方式：**Arduino**，分步骤推进，每步都有可验证的结果。

---

## 硬件与软件准备

### 硬件清单
- ESP32-C6 开发板
- 1.3 寸 ST7789 LCD（SPI 接口，分辨率 240x240）
- 杜邦线若干
- 面包板（可选，方便接线）

### LCD 引脚（你的实际接线）
这是你确认的引脚定义，方案以此为准（ESP32 的 GPIO 矩阵支持任意引脚接 SPI，所以完全没问题）：

| LCD 信号 | 说明 | ESP32-C6 GPIO |
|---|---|---|
| VCC / 3.3V | 电源 | 3.3V |
| GND | 地 | GND |
| SCL / SCK | SPI 时钟 | GPIO 7 |
| SDA / MOSI | SPI 数据 | GPIO 6 |
| MISO | 未使用（ST7789 只写） | GPIO 5（可忽略） |
| CS | 片选 | GPIO 14 |
| DC | 数据/命令选择 | GPIO 15 |
| RST | 复位 | GPIO 21 |
| BLK / BL | 背光 | GPIO 22 |

> 如果你的模块丝印与上面命名略有出入（如 SDA=MOSI、RES=RST），按模块实际丝印接线即可。

### 软件环境（已更新：改用 LovyanGFX）
- **PlatformIO**（本机已装，版本 6.1.19）
- 显示库：**LovyanGFX**（原生支持 ESP32-C6 + ST7789，且内置 Sprite，适合后续动画）

> ⚠️ **为什么不用 TFT_eSPI？**（2026-08-27 实测）ESP32-C6 需要 arduino-esp32 3.x / IDF 5.x，而 TFT_eSPI 至今未合并 C6 支持（GitHub PR #3399 长期未合并），用 2.5.43 编译必报 GPIO 寄存器错误。LovyanGFX 是 TFT_eSPI 的现代替代品，API 风格相似（fillScreen / setTextColor / print 通用），动画能力还更强。

**库安装方式**（PlatformIO registry 下载在国内网络易卡死，故手动放置）：
- 库源码位于项目 `lib/LovyanGFX/`（从 GitHub 下载 zip 解压，已重命名去掉 `-master` 后缀）
- 若需更新，重新下载 `https://github.com/lovyan03/LovyanGFX` 的 master 分支 zip 替换即可

### 关键配置：引脚直接在代码里配
LovyanGFX 不需要改库文件，引脚在 `src/main.cpp` 顶部的 LGFX 配置类中直接设置（见代码注释）：

```cpp
class LGFX : public lgfx::LGFX_Device {
    lgfx::Panel_ST7789  _panel_instance;   // 面板型号
    lgfx::Bus_SPI       _bus_instance;     // SPI 总线
    lgfx::Light_PWM     _light_instance;   // 背光（PWM）

    // 总线：SPI2_HOST, 40MHz
    // 引脚：SCLK=7, MOSI=6, DC=15, CS=14, RST=21, BL=22
    // 面板：240x240, offset 0,0
};
```

> 引脚若调整，只需改 `src/main.cpp` 中对应 `cfg.pin_xxx` 一行即可。

**实际工程配置（platformio.ini，已在项目中创建）：**

```ini
; PlatformIO 工程配置：ESP32-C6 + 1.3" ST7789 LCD
; 显示库：LovyanGFX（原生支持 ESP32-C6 + ST7789 + Sprite 动画）
; 库位于 lib/LovyanGFX（GitHub 直连慢，已手动下载放入本地库目录）
[env:esp32-c6]
platform = espressif32
board = esp32-c6-devkitc-1
framework = arduino
monitor_speed = 115200
```

> 注：Linux/macOS 用户不适用 `board = esp32-c6-devkitc-1` 是 ESP32-C6 通用板定义；若你的板子型号特殊（如合宙 Air101 系列），需换成对应 `board` 名称。

---

## 分步实施计划

### 步骤 1 —— 点亮屏幕（验证硬件 + 驱动）✅ 已完成
**做什么**：初始化屏幕，刷满一种颜色，再显示一行文字。
**为什么**：先确认接线、引脚、驱动都正确，再谈动画。这是所有后续步骤的地基。

**实现**：`src/main.cpp` 使用 LovyanGFX（LGFX 配置类内含全部引脚），状态：**编译通过**（flash 28.2%，RAM 5.2%）。

**验收标准**：
- 屏幕能显示纯色（红→绿→蓝闪烁）
- 屏幕能显示一行文字（"Hello ST7789!"）
- 背光正常点亮

**待你确认**：烧录后告诉我屏幕上看到什么（颜色顺序？文字清晰？有无偏色/负片？）。

**预计完成时间**：代码已就绪，等待你烧录验证。

---

### 步骤 2 —— 静态画面：画出"山"的背景
**做什么**：不再刷单色，而是画出一个简单的场景：天空渐变 + 山坡 + 地面。
**为什么**：动画需要背景层，先单独把背景做出来，好看又直观。

**会用到的 LovyanGFX API**（与 TFT_eSPI 同名，资料通用）：
- `fillRect()` 画矩形块（地面）
- `fillTriangle()` 画三角形（山体轮廓）
- `fillCircle()` 画太阳/云朵

**验收标准**：
- 屏幕上有天空、山、地面的清晰分层
- 画面稳定无闪烁

---

### 步骤 3 —— 静态小人（精灵）
**做什么**：在画面上画出一个"小人"角色。
**为什么**：动画的核心是移动一个角色，先把角色本身做好。

**实现思路**：
- 先用简单"火柴人"（头部一个圆 + 身体和四肢用矩形/线条）快速验证
- 后续可升级为多帧像素小人

**验收标准**：
- 画面上能看到一个结构清晰的小人
- 小人位置可以手动改坐标观察

---

### 步骤 4 —— 让小人动起来（动画循环）
**做什么**：让小人从左下角沿路径移动到右上角，模拟爬坡。
**为什么**：这是你真正的目标——动态特效。

**实现思路**：
1. 主循环 `loop()` 里维护小人的坐标 `(x, y)`
2. 每帧：清掉旧位置 → 更新坐标（x 增加、y 随山坡降低）→ 新位置画小人
3. 帧间 `delay(30~50ms)`，约 20~30fps
4. 到达右上角后可以循环回左下角重新开始

**运动轨迹设计**（核心，稍后可以细化）：
- 简单版：直线从左下角 `(20, 200)` 到右上角 `(210, 40)`
- 进阶版：让 y 跟随一条"山脊"曲线，走起来像真的在爬山

**验收标准**：
- 小人能连续、流畅地从左下角移动到右上角
- 无明显闪烁（双缓冲可选优化）

---

### 步骤 5 —— 打磨：行走动画 + 特效（可选增强）
**做什么**：让效果更生动。
**可选子项**：
- 多帧行走动画（腿部交替摆动）
- 小人随地形起伏
- 云朵缓慢移动 / 太阳光晕
- 到达山顶时的庆祝效果
- 帧率优化（减少闪烁）

**验收标准**：视觉上更自然、更有"动态特效"的感觉。

---

## 里程碑规划（推进节奏）

| 阶段 | 内容 | 状态 |
|---|---|---|
| 准备 | 接线确认（SCLK=7, MOSI=6, CS=14, DC=15, RST=21, BL=22）+ 装 LovyanGFX | ✅ 已完成 |
| 步骤 1 | 点亮屏幕（黑底白字，`invert=true` 已确认） | ✅ 已完成 |
| 步骤 2 | 静态山景背景（天空+太阳+云+远山+前景山坡+草地） | ✅ 已完成（已修正太阳位置） |
| 步骤 3 | 静态小人（`drawPerson()` 可复用函数） | 🔄 已烧录，待验收 |
| 步骤 4 | 动画移动（核心目标） | 待开始 |
| 步骤 5 | 打磨特效（可选） | 待开始 |

> 我们按顺序一步步来，每完成一步、确认没问题了，再进入下一步。不要一次做完。

---

## 烧录 & 验证方法
- 烧录：接线完成后，在项目目录运行 `pio run -t upload`
- 查看串口日志：`pio device monitor`（波特率 115200），能看到 `[Step1] done...` 即正常
- 若无法烧录（找不到 COM 口）：检查 USB 连接、驱动（CP210x/CH340），或 `pio device list` 查看可用端口

## 备注 / 常见问题
- **屏幕不亮**：先查背光（BL=22）是否接对，再查 VCC 是否 3.3V（不要接 5V）。
- **花屏/乱码**：多半是引脚映射或驱动型号配置错误，回 `src/main.cpp` 里的 LGFX 配置类检查。
- **颜色偏色/负片**：在 `src/main.cpp` 的 LGFX 配置中改 `cfg.invert = true/false`。
- **闪烁**：属于正常现象，步骤 4/5 时会用 Sprite 双缓冲优化。
- 若你的模块不是 240x240，或者引脚不同，告知我，我帮你调整方案。
