# 小人精细化方案 · 脖子修复 + 步行循环动画

> 目标：把当前"正面站桩、整体平移"的火柴人，升级为**侧身攀爬、四肢协同摆动**（摆臂 + 迈腿）的小人。
> 状态：**方案待确认**（本篇只写方案与理解，未改代码）
>
> 分两步推进：**先做阶段 1（脖子 + 摆臂）**，网页验收通过后，再做阶段 2（迈腿 + 对角协同）。

---

## 1. 当前小人为什么"奇怪"

### 问题 A：脖子不见了

看当前坐标：

```
头：中心 (cx+2, gyMid-26)，半径 8
    → 头顶 gyMid-34，头底 gyMid-18
肩：躯干起点 (cx+4, gyMid-19)
```

**头底（gyMid-18）和肩膀（gyMid-19）几乎贴在一起**，只有 1px 空隙。
正面姿势时双臂从肩部两侧平展，视觉上盖住了这个问题；改侧身后躯干集中，头肩就"焊"在一起，看起来没有脖子。

### 问题 B：整个人在动，四肢不动（刚性平移）

```
tick(stepPx):
    _personX += stepPx;   // 只移动小人整体 x 坐标
    drawFrame(_personX);  // 姿态完全不变，整体搬走
```

小人的所有部位（头/手/腿）坐标只由整体位置 `cx` 决定，
**没有任何"随时间变化"的部件** → 看起来像被整个平移，而不是在走路。

---

## 2. 解决方案：步行相位（walk cycle）

### 核心思路

给小人加一个**相位角 phase**，随移动距离周期性推进（0°→360°→循环）：

```
phase 随小人前进距离增长，停下即静止
一个完整摆臂周期 = 走一步
```

四肢按不同相位规律绕关节摆动：

```text
右手（前臂）绕肩前后摆：  手位置 = 基本位置 + A × sin(phase)
左手（后臂）绕肩前后摆：  手位置 = 基本位置 - A × sin(phase)   ← 相位差 180°（交替）
```

- phase = 0°：右手前伸、左手后甩
- phase = 90°：双手回中立
- phase = 180°：左手前伸、右手后甩
- phase = 270°：再回中立

### 为什么能做到"迈左腿伸右臂"

因为走路时手臂和腿是**对角线协同**的：

```
左腿相位 = 右臂相位（左脚前时右手前伸）
右腿相位 = 左臂相位
```

阶段 2 做腿部时按此规则对接，即得自然步态。

---

## 3. 分阶段实施

### 阶段 1：脖子修复 + 摆臂（本次做）

| 子项 | 改动 | 验收 |
|---|---|---|
| 脖子 | 头中心从 gyMid-26 上移到 gyMid-28，头肩间留 2~3px；画一条下巴→肩的短颈线 | 头"长"在脖子上，不再贴在躯干 |
| 相位变量 | 新增 `walkPhase`，随 `tick()` 移动距离推进（走多少像素相位走多少，停下静止） | 暂停时小人静止在某个姿态 |
| 摆臂 | 手臂保持两段式（肩→肘→手），"手"绕肩前后摆动，幅度 3~6px，肘跟随 | 播放时双手交替前后摆，像走路 |
| 同步 | `LCD-simulator.html` 与 `src/MountainAnim.cpp` 坐标/逻辑一致 | 模拟器验收通过 + 固件编译通过 |

**验收方式**：打开 `LCD-simulator.html` 播放，观察：
- [ ] 脖子可见，头与躯干分离
- [ ] 双手交替前后摆臂（相位差 180°）
- [ ] 摆臂幅度自然（非划水、非僵直），可调
- [ ] 暂停时小人停在当前姿态，不自动摆动

### 阶段 2：迈腿 + 对角协同（验收后做）

| 子项 | 改动 | 验收 |
|---|---|---|
| 迈腿 | 双腿按相位前后摆动；前脚落高处（gyR）、后脚踩低处（gyL），保持贴合坡面 | 播放时双腿交替迈步 |
| 对角协同 | 左腿相位 = 右臂相位，右腿相位 = 左臂相位 | 迈左腿伸右臂，步态自然 |
| 膝盖弯曲 | 保持或增强膝盖两段式弯曲，随相位变化 | 步伐有"抬起-落下"感 |

**验收方式**：播放观察：
- [ ] 双腿交替迈步，前脚踩高处、后脚踩低处，始终贴合坡面
- [ ] 迈左腿时右臂前伸（对角协同）
- [ ] 整体步伐与速度匹配（走多快摆多快）

---

## 4. 设计细节备忘

- **摆臂幅度**：3~6px（小人整体约 37px 高，太大像划水，太小看不出）
- **摆动方向**：手臂绕肩**前后**摆（沿 x 方向），不是上下
- **相位推进**：`phase += stepPx / stride`（stride = 每步移动像素数）→ 速度变快相位自然变快，暂停即静止
- **兼容性**：`drawFrame(x)` 手动定位（进度条场景）也能用相位——由宿主决定是否推进相位；默认 tick 自动推进
- **透明推屏 / 宿主区 / 山体**：完全不动

---

## 5. 涉及文件

| 文件 | 改动范围 |
|---|---|
| `LCD-simulator.html` | `drawPerson()` 增加相位参数与摆臂逻辑；动画循环传入相位 |
| `src/MountainAnim.cpp` | `drawPerson()` 同步；`tick()` 推进 `walkPhase`；新增成员 `_walkPhase` |
| `src/MountainAnim.h` | 新增 `walkPhase` 相关成员/可选接口 |
| `MOUNTAIN_ANIM_README.md` | （可选）文档补充摆臂/步态说明 |

---

## 6. 待确认

1. **阶段顺序**：先脖子 + 摆臂，验收后做迈腿 —— 对吗？
2. **摆臂幅度**：先按 3~6px 实现，可在模拟器里调，可以吗？
3. **步幅（stride）**：默认每走 24px 完成一个摆臂周期（约一步），不合适再调。

> 确认后开始阶段 1 实现。