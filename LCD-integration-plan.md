# LCD 爬坡动画 · 局部画布集成方案

> 目标：把"小人爬坡动画"改造成**只占用屏幕下方 1/3 的局部画布**，以便嵌入到其他项目中——宿主项目自由使用上方 2/3 区域（UI、文字、数据、其他动画），互不干扰。
>
> 状态：**方案待确认**（本篇只写方案，未改代码）

---

## 1. 为什么需要改造

### 现状（全屏画布的问题）

当前 `src/main.cpp` 每帧做**整屏重绘**：

```cpp
lgfx::LGFX_Sprite canvas(&tft);
canvas.createSprite(240, 240);   // 画布 = 全屏
...
canvas.pushSprite(0, 0);         // 整屏推上屏幕
```

并且 `drawScene()` 每帧重画 4 层天空色带（y=0~180）+ 山体（y=180~240）：

```cpp
canvas.fillRect(0, 0,   240, 45, C_SKY_TOP);
canvas.fillRect(0, 45,  240, 45, C_SKY_2);
...
```

**后果**：动画虽然"内容"只占下方 1/3，但绘制动作覆盖全屏——宿主在上方画的内容，每帧都被天空渐变刷掉。这就是你说的"如果上面有内容，动画会不会影响"的答案：**现在的写法会覆盖，需要改造才不会覆盖。**

### 关键 API 支持（已确认）

LovyanGFX 的 Sprite 推屏支持**透明色按键**：

```cpp
canvas.pushSprite(int32_t x, int32_t y, const T& transp);   // transp = 透明色
// 推屏时，画布中颜色 == transp 的像素被跳过（不写入屏幕）
// 屏幕该位置原本的内容会保留下来
```

这正是本方案的基础：**动画画布缩到下方 1/3，山体以上的空白区域填透明色，推屏时透明像素自动放行，宿主画面不受影响。**

---

## 2. 核心方案

| 项 | 改造前 | 改造后 |
|---|---|---|
| 画布尺寸 | 240 × 240（全屏） | **240 × 80**（仅下方 1/3） |
| 画布推屏位置 | `pushSprite(0, 0)` | `pushSprite(0, 160)` |
| 天空背景 | 动画画（y 0~180） | **动画不再画**（交给宿主） |
| 山 + 小人 | 屏幕绝对坐标 | **局部坐标**（y 整体减 160） |
| 画布空白区 | 天空色 | **透明色（0x0000）** |
| 宿主上方内容 | 每帧被覆盖 | **完全保留** |

### 屏幕坐标 ↔ 动画画布坐标

动画区域 = 屏幕 y∈[160, 240]（下方 1/3），画布局部 y' = 屏幕 y − 160：

| 内容 | 屏幕坐标 | 画布局部坐标 (y−160) |
|---|---|---|
| 动画区左上角 | (0, 160) | (0, 0) |
| 山脚（左） | (0, 240) | (0, 80) |
| 山顶平台 | (202,198)~(215,198) | (202, 38)~(215, 38) |
| 山脚（右） | (240, 240) | (240, 80) |
| 小人脚（x=100 时） | (100, ≈224) | (100, ≈64) |

> 山体本身就在 y=180~240，减去 160 后全部落在画布 y'=20~80，**完全在局部画布内**。

---

## 3. 代码改造（src/main.cpp）

### 3.1 画布改为局部

```cpp
LGFX tft;
lgfx::LGFX_Sprite canvas(&tft);   // 离屏缓冲（双缓冲，防闪烁）

// 新增：动画区域常量
static const int ANIM_H = 80;          // 下方 1/3 高度 = 240 - 160
static const int ANIM_Y = 160;         // 动画区在屏幕上的起始 y
static const uint16_t C_TRANSP = 0x0000;  // 透明色（黑），动画中不得使用纯黑
```

`setup()` 里：

```cpp
canvas.setColorDepth(16);
canvas.createSprite(240, ANIM_H);    // 240 x 80，不再全屏
```

### 3.2 山脊数据改局部坐标

RIDGE 的 y 全部减 160（山脚 = 画布底 80，山顶平台 y' = 38）：

```cpp
static const int RIDGE[][2] = {
    {  0,  80},   // 山脚（左下角）屏幕 y=240
    { 45,  73},
    { 90,  65},
    {130,  58},
    {150,  60},   // 小凹
    {175,  48},
    {190,  40},
    {202,  38},   // 进入山顶（左侧）
    {215,  38},   // ← 山顶平坦段（水平台地）
    {225,  48},   // 右侧下坡
    {240,  80},   // 山体右缘（闭合用）
};
```

### 3.3 drawScene()：去掉天空，山体以上填透明

```cpp
static void drawScene()
{
    // 0) 整张画布先填透明色（替代原来的天空）
    canvas.fillRect(0, 0, 240, ANIM_H, C_TRANSP);

    // 1) 山体主轮廓（局部坐标，沿折线向下填到底 = 画布底 y=80）
    for (int i = 0; i < RIDGE_N - 1; i++) {
        int x1 = RIDGE[i][0],   y1 = RIDGE[i][1];
        int x2 = RIDGE[i+1][0], y2 = RIDGE[i+1][1];
        canvas.fillTriangle(x1, y1, x2, y2, x2, 80, C_MOUNTAIN);
        canvas.fillTriangle(x1, y1, x2, 80, x1, 80, C_MOUNTAIN);
    }

    // 2) 岩层 / 雪帽 / 裂缝 / 山脊线：与现有一致，仅 y 坐标整体减 160
    //    （雪帽顶点局部 y ≈ 28~44；裂缝局部 y ≈ 10~71；岩层 drop 不变）
    //    详见当前 main.cpp drawScene() 逐行平移
}
```

### 3.4 pushSprite 透明推屏

```cpp
static void drawFrame(int personX)
{
    drawScene();

    // 小人脚的地面高度：groundAt() 也返回局部坐标（内部 RIDGE 已改）
    int gyL = groundAt(personX - 6);
    int gyR = groundAt(personX + 6);
    drawPerson(personX, gyL, gyR, C_PERSON);

    // 关键：第三个参数 = 透明色；透明像素跳过，不覆盖宿主内容
    canvas.pushSprite(0, ANIM_Y, C_TRANSP);
}
```

> ⚠️ 注意：`groundAt()` / `drawPerson()` 用的都是局部坐标，**只需 RIDGE 改了，两者自动适配**（它们基于 RIDGE 计算）。

### 3.5 循环逻辑不变

```cpp
void loop()
{
    for (int x = 20; x <= 205; x += 2) {
        drawFrame(x);
        delay(30);
    }
    delay(1500);
}
```

---

## 4. 推荐：封装成独立模块（给其他项目用）

不直接改 `main.cpp` 的宿主，而是把动画抽成一个**可复用模块**，对方只需 3 行：

**`MountainAnim.h`**

```cpp
#pragma once
#include <LovyanGFX.hpp>

// 爬坡动画模块：只占用屏幕下方 1/3，不干扰宿主上方内容
class MountainAnim {
public:
    void init(lgfx::LGFX_Device* tft);   // 传入宿主屏幕实例，创建局部画布
    void tick();                         // 每帧调用：推进一帧并推屏（透明）
    // 可选：手动控制位置（步进动画用）
    void setPersonX(int x);
    void drawFrame(int x);
private:
    static const int W = 240;
    static const int H = 80;             // 下方 1/3
    static const int OFFSET_Y = 160;     // 屏幕上的起始 y
    lgfx::LGFX_Sprite canvas;
    int personX = 100;
};
```

**宿主项目用法**

```cpp
MountainAnim anim;

void setup() {
    tft.init();
    anim.init(&tft);          // ① 初始化
}

void loop() {
    // ② 宿主自己画上方 2/3 的内容（任意 UI / 文字 / 数据）
    drawMyUpperUI(&tft);      // ← 每帧都不会被动画覆盖

    anim.tick();              // ③ 动画只动下方 1/3
    delay(30);
}
```

---

## 5. 注意事项

1. **透明色不能出现在动画画面里**
   透明色选 `0x0000`（纯黑）最安全——动画只有绿山、白雪、红人、白棱线，没有纯黑。若宿主在动画区下方/内部画了纯黑，可用一个颜色专属常量并保证动画不出现。

2. **动画区上部有空隙**
   改造后动画画布是 240×80，山体实际从 y'=20 开始。y'=0~19（屏幕 160~179）是透明区——如果宿主希望这里也显示背景（而不是透出下方内容），由**宿主自己画**这片区域，动画不会碰。

3. **不要用 `fillScreen`**
   透明推屏依赖"画布透明区颜色 == transp"，`fillRect(0,0,240,H,C_TRANSP)` 每帧打底即可，不要把不受控的旧帧残留画进画布。

4. **旋转与坐标**
   若宿主换了屏幕方向（`setRotation`），动画区位置 (0,160) 需按旋转后的布局重新对齐；推荐由宿主提供"动画区矩形"（x, y, w, h）给模块，模块不写死 160。

5. **性能**
   推屏从全屏 240×240 缩小到 240×80，SPI 传输量减少 2/3，动画帧率只会更高。

---

## 6. 验收标准

- [ ] 编译通过（`pio run`）
- [ ] 单独运行：动画照旧，小人爬坡流畅，画面无闪烁
- [ ] 联合测试：在 `loop()` 里先画上方内容（如 "HELLO" 大字/色块），动画运行时**上方内容全程不受影响**
- [ ] 动画区上边缘干净：屏幕 y=160 这条线上方无山体/小人物溢出
- [ ] 山体边缘与透明区过渡无残影（每帧透明打底生效）

---

## 7. 实施步骤（分 4 步推进，每步验证后再进下一步）

> 我们按 A → B → C → D 的顺序一步步来，每步都在真实 LCD 上验证通过后，再进入下一步。不要一次做完。

### 步骤 A：画布缩小到下方 1/3（能不能"只画下边"）

**改动**（`src/main.cpp`）：
- 画布 `createSprite(240, 240)` → `createSprite(240, 80)`（只覆盖屏幕 y=160~240）
- RIDGE 山脊点 y 整体减 160（改用局部坐标，见第 3.2 节）
- `drawScene()` 去掉天空层，先 `fillRect(0, 0, 240, 80, C_TRANSP)` 透明打底（见第 3.3 节）

**验收标准**：
- [ ] `pio run` 编译通过
- [ ] 烧录后动画照常，小人爬坡流畅
- [ ] 山 + 人完整落在屏幕 y=160~240，边缘无残影
- [ ] 动画区上边缘（y=160 附近）无山体/小人溢出

### 步骤 B：透明推屏 —— 验证上面内容不被覆盖

**改动**（`src/main.cpp`）：
- `canvas.pushSprite(0, 0)` → `canvas.pushSprite(0, ANIM_Y, C_TRANSP)`（透明像素跳过，见第 3.4 节）
- 加一段"宿主模拟测试"：在 `loop()` 里每帧先画上方内容（红/绿大色块 + "HELLO" 文字），再跑动画

**验收标准**：
- [ ] 动画运行期间，上方色块和文字**全程保持清晰**，不被动画刷新掉
- [ ] 透明区过渡干净，无阴影/残影

### 步骤 C：封装成可复用模块（给别人用）

**改动**：
- 抽出 `src/MountainAnim.h` + `src/MountainAnim.cpp`（颜色、RIDGE、绘制、步进封装成类，见第 4 节）
- `src/main.cpp` 改为调用 `MountainAnim` 类

**验收标准**：
- [ ] 行为与步骤 B 完全一致（动画、透明、上方不受影响）
- [ ] 第三方只需 `init(&tft)` + `tick()` 三行接入（文档示例可编译）

### 步骤 D：参数化 + 收尾

**改动**：
- 接口加 `init(tft, x, y, w, h)`，动画区位置/大小可配置（不只写死 (0,160) 240×80）
- 更新 README、`LCD-integration-plan.md`、`LCD-animation-plan.md`
- 全部改动提交推送到 GitHub

**验收标准**：
- [ ] 改参数（如换成 y=170 或 200×60）动画区正确跟随
- [ ] 文档与代码一致
- [ ] Git 已提交推送（commit + push）

---

> 如果你希望模块的位置/尺寸从一开始就参数化（不只是固定 (0,160) 240×80），告诉我，我在第 4 节接口里直接加 `init(tft, x, y, w, h)`，步骤 A 就按参数化写。